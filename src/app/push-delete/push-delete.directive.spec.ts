import { PushDeleteDirective } from './push-delete.directive';

describe('PushDeleteDirective', () => {
  it('should create an instance', () => {
    const directive = new PushDeleteDirective();
    expect(directive).toBeTruthy();
  });
});
